<UserControl
  x:Class="Winser.Views.HistoryView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:models="using:Winser.Models"
  xmlns:vm="using:Winser.ViewModels"
  mc:Ignorable="d">

  <UserControl.Resources>
    <!--  Source and the ListView hookup are set in code: x:Bind is not evaluated inside a
          ResourceDictionary, and CollectionViewSource.View is not an observable property.  -->
    <CollectionViewSource x:Name="GroupedHistory" IsSourceGrouped="True" />
  </UserControl.Resources>

  <Grid Background="{ThemeResource WinserContentBackgroundBrush}" Padding="32,28,32,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Style="{StaticResource PageHeaderTextStyle}" Text="History" />
        <TextBlock Opacity="0.65" Text="Pages you have visited on this device." />
      </StackPanel>
      <Button
        Grid.Column="1"
        Click="OnClearAll"
        Content="Clear all history"
        VerticalAlignment="Bottom" />
    </Grid>

    <AutoSuggestBox
      Grid.Row="1"
      Margin="0,20,0,12"
      MaxWidth="420"
      HorizontalAlignment="Left"
      PlaceholderText="Search history"
      QueryIcon="Find"
      Text="{x:Bind ViewModel.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock
      Grid.Row="2"
      Margin="0,32,0,0"
      HorizontalAlignment="Center"
      Opacity="0.6"
      Text="Nothing here yet."
      Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

    <ListView
      x:Name="HistoryList"
      Grid.Row="2"
      IsItemClickEnabled="True"
      ItemClick="OnEntryClick"
      SelectionMode="None">
      <ListView.GroupStyle>
        <GroupStyle HidesIfEmpty="True">
          <GroupStyle.HeaderTemplate>
            <DataTemplate x:DataType="vm:HistoryGroup">
              <TextBlock
                Margin="0,16,0,4"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind Key}" />
            </DataTemplate>
          </GroupStyle.HeaderTemplate>
        </GroupStyle>
      </ListView.GroupStyle>
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:HistoryEntry">
          <Grid Padding="0,6" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon
              FontSize="14"
              Glyph="&#xE774;"
              Opacity="0.55"
              VerticalAlignment="Center" />
            <StackPanel Grid.Column="1">
              <TextBlock Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
              <TextBlock
                Opacity="0.55"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Url}"
                TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <TextBlock
              Grid.Column="2"
              Margin="8,0"
              Opacity="0.55"
              Style="{StaticResource CaptionTextBlockStyle}"
              Text="{x:Bind LastVisitedUtc, Converter={StaticResource RelativeTime}}"
              VerticalAlignment="Center" />
            <Button
              Grid.Column="3"
              Click="OnRemoveEntry"
              Content="&#xE74D;"
              Style="{StaticResource ToolbarIconButtonStyle}"
              ToolTipService.ToolTip="Remove from history" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</UserControl>
