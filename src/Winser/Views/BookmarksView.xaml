<UserControl
  x:Class="Winser.Views.BookmarksView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:models="using:Winser.Models"
  mc:Ignorable="d">

  <Grid Background="{ThemeResource WinserContentBackgroundBrush}" Padding="32,28,32,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,16" ColumnSpacing="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock Style="{StaticResource PageHeaderTextStyle}" Text="Bookmarks" />
        <TextBlock Opacity="0.65" Text="Give a bookmark a folder to group it on the bookmarks bar." />
      </StackPanel>
      <Button
        Grid.Column="1"
        Click="OnAddCurrentPage"
        Content="Bookmark this tab"
        VerticalAlignment="Bottom" />
    </Grid>

    <TextBlock
      Grid.Row="1"
      Margin="0,32,0,0"
      HorizontalAlignment="Center"
      Opacity="0.6"
      Text="No bookmarks yet. Press Ctrl+D on a page to add one."
      Visibility="{x:Bind Items.Count, Mode=OneWay, Converter={StaticResource EmptyToVisibility}, ConverterParameter=Invert}" />

    <ListView
      Grid.Row="1"
      IsItemClickEnabled="True"
      ItemClick="OnBookmarkClick"
      ItemsSource="{x:Bind Items, Mode=OneWay}"
      SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:Bookmark">
          <Grid Padding="0,8" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <FontIcon
              FontSize="14"
              Glyph="&#xE735;"
              Opacity="0.6"
              VerticalAlignment="Center" />

            <StackPanel Grid.Column="1">
              <TextBlock Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
              <TextBlock
                Opacity="0.55"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Url}"
                TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <Border
              Grid.Column="2"
              Padding="8,2"
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              CornerRadius="10"
              VerticalAlignment="Center"
              Visibility="{x:Bind Folder, Converter={StaticResource EmptyToVisibility}}">
              <TextBlock
                FontSize="11"
                Opacity="0.8"
                Text="{x:Bind Folder}" />
            </Border>

            <Button
              Grid.Column="3"
              Click="OnEditBookmark"
              Content="&#xE70F;"
              Style="{StaticResource ToolbarIconButtonStyle}"
              ToolTipService.ToolTip="Edit" />
            <Button
              Grid.Column="4"
              Click="OnRemoveBookmark"
              Content="&#xE74D;"
              Style="{StaticResource ToolbarIconButtonStyle}"
              ToolTipService.ToolTip="Remove" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</UserControl>
